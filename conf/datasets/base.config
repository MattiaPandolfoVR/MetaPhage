params {
  config_profile_name = 'Base profile'
  config_profile_description = 'Standard profile for working with the pipeline'
  // output dirs
  params.outdir = "./output"
  params.temp_dir = "./tmp"
  // inputs
  singleEnd = false
  readPaths = './datasets/base'
}
